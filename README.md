# ludicrousstudio pages

Public GitHub Pages site for Ludicrous Studio.

- `gemstone-cascade/` — privacy policy, terms, and support pages for
  Gemstone Rhapsody (URLs referenced by the iOS app and its App Store
  listing — do not move or rename these files).

Source of truth: the private `gemstone-cascade` repo (files copied from its
`main`). Update there first, then copy here.
